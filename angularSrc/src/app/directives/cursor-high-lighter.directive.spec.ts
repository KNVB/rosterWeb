import { CursorHighLighterDirective } from './cursor-high-lighter.directive';

describe('CursorHighLighterDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorHighLighterDirective();
    expect(directive).toBeTruthy();
  });
});
