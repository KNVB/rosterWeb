export default class SelectedRegionUtil{
    constructor(rosterTableData){
        let selectedRegion=new SelectedRegion();
        let copiedRegion=new CopiedRegion();
        let itoCount=Object.keys(rosterTableData.rosterList).length;


    }
}