import {useContext} from 'react';
import RosterWebContext from '../../utils/RosterWebContext';
export default function UnreRosterRow(props){
    let {monthlyCalendar,rosterList}=useContext(RosterWebContext);
    
    return(
        <tr>

        </tr>
    )
}