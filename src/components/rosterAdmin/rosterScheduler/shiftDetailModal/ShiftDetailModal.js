import { Button, Modal } from 'react-bootstrap';
export default function ShiftDetailModal(
    {
        hideShiftDetail, isShowShiftDetail,
        selectedITOId, selectedShiftDetailDate,
        uiAction
    }) {

}